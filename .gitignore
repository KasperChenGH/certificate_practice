__pycache__/
*.pyc
_tmp_*
*.bak
.DS_Store

# Design loop artefacts: screenshots and the derived standalone previews.
# The .dc.html sources and the generators are committed; these are regenerable.
shot-*.png
crop-*.png
design/_preview.*.html
# The seeded design canvas: 2.2 MB of editor payload, regenerable from the
# .dc.html sources, and it would otherwise be served publicly from the site root.
design/certifications-courses-landing.html
